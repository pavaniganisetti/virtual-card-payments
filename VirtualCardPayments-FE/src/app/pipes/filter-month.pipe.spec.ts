import { FilterMonthPipe } from './filter-month.pipe';

describe('FilterMonthPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterMonthPipe();
    expect(pipe).toBeTruthy();
  });
});
